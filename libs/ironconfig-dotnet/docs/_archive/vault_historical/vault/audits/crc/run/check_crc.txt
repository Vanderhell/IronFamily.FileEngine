Analyzing the golden BJV2 CRC file...

File size: 161 bytes
Header shows:
- CRC offset: 157 (0x9D)
- This means bytes 157-160 contain the CRC

When I regenerated the file, the CLI said:
"Size: 161 bytes"

But the CRC validation is failing with:
"expected 8AF422D1, got EEB1BF1F"

This suggests the CRC computed during parsing doesn't match what was stored during encoding.

Possible issues:
1. The endianness of how I'm reading the stored CRC
2. The endianness of how I'm writing the CRC
3. The Crc32Ieee.Compute implementation has a bug
4. The data being hashed is different between encoding and parsing

Since the test vector (123456789) = 0xCBF43926 passed, the Crc32Ieee implementation should be correct.

Let me check if the issue is with how BitConverter handles endianness on this system.
